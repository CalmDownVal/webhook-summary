{
	"name": "@calmdownval/webhook-summary",
	"version": "1.0.1",
	"description": "A GitHub Action for simple pipeline summaries sent via webhooks.",
	"license": "MIT",
	"scripts": {
		"build": "esbuild --bundle --platform=node --target=node16 --minify --sourcemap --outfile=./build/index.cjs ./src/index.mjs"
	},
	"dependencies": {
		"@actions/core": "1.8.0",
		"@actions/github": "5.0.1",
		"@actions/http-client": "1.0.11"
	},
	"devDependencies": {
		"esbuild": "0.14.38"
	}
}
